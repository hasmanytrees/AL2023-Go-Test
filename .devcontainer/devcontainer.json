{
    "name": "AL2023-Go",
    "build": {
        "dockerfile": "Dockerfile"
    },
    "remoteUser": "root",
    "mounts": [
        {"type": "bind", "source": "${localEnv:HOME}/.ssh", "target": "/root/.ssh"}
    ],
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-azuretools.vscode-docker",
                "golang.Go"
            ]
        }
    },
    "onCreateCommand": {
        "go": "yum -y install go && go install -v golang.org/x/tools/gopls@latest && go install github.com/go-delve/delve/cmd/dlv@latest && go install honnef.co/go/tools/cmd/staticcheck@latest"
    }
}